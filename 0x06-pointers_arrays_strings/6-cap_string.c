/**
 * 6-cap_string
 */


#include "main.h"
#include <stdio.h>

/**
 * cap_string: Capitalize all words of a string
 * @s
 *
 * Return : char value
 */
char *cap_string(char *s)
{
        int a = 0, i;
        int cspc = 13;
        char spc[] = {32, '\t', '\n', 44, ';', 46, '!', '?', '"', '(', ')', '{', '}'};

        while (s[a])
        {
-- INSERT --         

